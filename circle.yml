machine:
  node:
    version: 5.7.0

dependencies:
  override:
    - npm install
    - npm run compile

test:
  override:
    - npm test

deployment:
  npm:
    branch: master
    commands:
      - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
      - npm run publish2npm